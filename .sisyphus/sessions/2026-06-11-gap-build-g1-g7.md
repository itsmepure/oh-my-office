# Session Handoff — 2026-06-11 (Gap Build G1–G7)

## Posisi Sekarang
Gap-closing build G1–G7 hampir tuntas. Plan: docs/GAP_BUILD_PLAN.md.
G1–G5 + G7 SELESAI & gate hijau. G6 (pixel office) nunggu penilaian visual user.
Sebelumnya: Monetization M0–M5 selesai (handoff m0-m2 + m3-m5).

## Yang Dibangun (G1–G7)

### G1 — Task output: view + download (VERIFIED end-to-end)
- packages/db/src/workspace.ts (@repo/db/workspace): listWorkspaceFiles, readWorkspaceFile, getWorkspaceRoot. Tenant-scoped (membership) + REUSE guardPath dari @repo/agents (PATH GUARD — bukan reimplement). db sekarang depend @repo/agents (acyclic, agents ga import db).
- API: GET /api/offices/[id]/files (list), /files/download?path= (stream+guard 403), /files/zip (adm-zip), /api/tasks/[taskId]/artifacts/[artifactId]/download.
- UI: workspace-files.tsx panel di office page (list+download+zip), download button di task-detail artifact rows.
- adm-zip + @types/adm-zip ditambah ke apps/web.
- Verified: curl list 4 files, download calc.py 200/198B isi benar, path escape ../../ → 403.

### G2 — Office rename + delete
- offices.ts: renameOffice (owner-only, trim, max120), deleteOffice (owner-only, cascade artifact/event/task/officeAgent/membership + rm workspace folder). import rm dari node:fs/promises.
- API: PATCH /api/offices/[id] (rename), DELETE (delete).
- UI: office-controls.tsx (rename inline + delete confirm) di office header, cuma kalau isOwner.

### G3 — Entitlement enforcement
- /api/agents POST: gate knowledge docs → canUseFullAgentBuilder, FREE+wantsKnowledge → 402.
- agents/new form: prop canUseKnowledge, FREE lihat upsell "Pro feature" ganti editor.
- dashboard: getEntitlements → "N / max offices", atCap → tombol jadi "Upgrade for more offices" + placeholder tile disembunyiin.

### G4 — Onboarding
- dashboard/onboarding-banner.tsx (client, localStorage dismiss key oo_onboarding_dismissed_v1): jelasin tasks unlimited / agent kita pakai credit / BYOK gratis + 3 CTA. Di-render top dashboard.

### G5 — Checkout wiring
- apps/web/src/lib/checkout.ts (server-only): buildCheckoutLinks(userId,email) → inject ?checkout[custom][user_id]+[email] ke semua link (pro/team/3 packs/portal). null kalau env ga ada.
- Settings page pakai buildCheckoutLinks (ganti inline CHECKOUT object).
- docs/MONETIZATION.md: section "Required env" lengkap (LEMON_WEBHOOK_SECRET, LEMON_VARIANT_*, LEMON_CHECKOUT_*, LEMON_PORTAL_URL).

### G7 — Hardening
- apps/web/src/lib/rate-limit.ts: in-memory token bucket per key + sweepRateLimiter. Wired ke /api/tasks POST (20 task/menit per user, 429 + Retry-After). +3 unit test.

## G6 — Pixel office (BELUM, nunggu user judge)
- Behavior system di pixel-office-scene.tsx SUDAH LENGKAP: DESK_SLOTS (4), ROAM_WAYPOINTS, walk/sit/work anim, state-driven dari reducer, anchor 0.9 (verified via composite). active=duduk di meja (working=workType anim, else sitStill), idle=roam waypoint+rest+breathe.
- Sisa cuma KALIBRASI VISUAL (posisi pixel meja persis, facing) — JANGAN dikejar pakai vision tool (kebukti flip-flop, failure loop). Gate G6 = USER sebagai juri, bukan vision. Tampilkan ke user, minta verdict.

## Gate (HIJAU, dev OFF)
typecheck 9/9, test 8/8 suites, lint 8/8, build 5/5.
Test counts: db 74 (+workspace 6, +office-lifecycle 5), web 54-ish (+rate-limit 3), orchestrator 26.

## PITFALL (penting)
- Docker Desktop bisa mati → Postgres down → orchestrator crash ::1:5432. Nyalain: powershell Start-Process 'C:\Program Files\Docker\Docker\Docker Desktop.exe', `docker start openoffice-db`. `docker compose up` ke-block tool guard → pakai `docker start`.
- Kill dev sebelum gate (daemon race test DB, terutama priority-queue/credit test).
- Vitest mock: route import subpath @repo/db/* baru ATAU @/lib/* → test yg vi.mock harus mock itu juga, else "Cannot find module" / "Failed to load url".
- write_file/patch konten besar bisa timeout → pecah kecil.
- Secret di .env jangan via heredoc (masking).

## Cara Resume
1. Docker ON → `docker start openoffice-db`. `pnpm dev`. Login demo@openoffice.local/demo1234.
2. Office demo office-demo-001 punya 4 file workspace (calc.py/greet2.py/index.html/ping.py) buat test G1.
3. Demo balance 497, FREE plan.

## SISA / NEXT
- G6: tampilkan pixel office ke user, kalibrasi posisi kalau user minta (user=juri).
- BELUM commit ke git (oh-my-office). .env JANGAN ke-commit.
- Operasional: akun Lemon Squeezy + isi env LEMON_* buat aktifin billing.
- Opsional (G7 lanjutan, belum): task-complete notif, password reset/email verif (butuh email infra).
